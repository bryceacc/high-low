{
	"name": "high-low",
	"version": "1.0.0",
	"description": "game of high low",
	"main": "high_low.js",
	"dependencies": {
		"express": "^4.13.4",
		"readline-sync": "^1.2.22",
		"socket.io": "^1.4.5",
		"socket.io-client": "^1.4.5"
	},
	"devDependencies": {
		"babel-cli": "^6.5.1",
		"babel-plugin-transform-react-jsx": "^6.5.2",
		"browserify": "^13.0.0",
		"react": "^0.14.7",
		"reactify": "^1.1.1",
		"uglify": "^0.1.5"
	},
	"scripts": {
		"build": "browserify -t reactify lib/ingame.js -o lib/bundle.js",
		"//update": "xcopy shit",
		"//clean": "mkdir dist && cd dist && mkdir js && mkdir css && cd ..",
		"prebuild": "babel src -d lib",
		"//postbuild": "uglifyjs dist/js/bundle.js -o src/js/bundle.min.js",
		"//preclean": "rmdir /S /q dist",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC"
}
